  <div class="my-5">
    <h2 class="text-2xl">Exercise:</h2><%= workout_exercise.exercise.name%>
  </div>

<div class="flex flex-column">
  <%= button_to 'Delete', workout_exercise_path(workout_exercise), method: 
  :delete, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium"%><%= link_to 'Add Set', new_workout_exercise_workout_set_path(workout_exercise), data: { turbo_frame: nested_dom_id(workout_exercise, WorkoutSet.new)}, class: "flex block rounded-lg mx-5 py-3 px-5 w-28 bg-blue-400 text-white block font-medium align-self-center"%> 
</div>

  <%= turbo_frame_tag nested_dom_id(workout_exercise, WorkoutSet.new) %>


  <%= turbo_frame_tag nested_dom_id(workout_exercise, "sets") do %>
      <%= render workout_exercise.workout_sets, workout_exercise: workout_exercise %>
  <% end %>

